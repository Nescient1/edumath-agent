# 含参函数讨论与导数证明不等式

## 含参函数讨论

含参函数题的关键是找到参数影响了哪一步：可能影响导数零点是否存在，也可能影响导数符号、极值或零点个数。

常见处理方法：

1. 先求导并化简。
2. 找出需要分类的临界情况，例如判别式为 0、参数为 0、临界点重合。
3. 按参数范围分别讨论单调性或最值。
4. 合并结论时注意边界值。

## 导数证明不等式

证明不等式时，可以把不等式移项，构造函数 g(x)，再通过导数研究 g(x) 的单调性或最值。

常见错误包括：只写导数不说明单调性，或得到最值后没有回到原不等式。
