大模型分类2. 大模型分类：效果更好

把 chunk 发给大模型，让它返回 JSON。

提示词可以这样写：

请判断下面文本属于高中数学哪个板块，并输出严格 JSON。

可选 module：
集合与逻辑、函数与导数、三角函数与解三角形、平面向量、数列、不等式、立体几何与空间向量、解析几何、概率统计、复数、计数原理、综合题与压轴题、其他。

可选 type：
concept、formula、method、example、solution、mistake、exercise、exam、summary、other。

请输出：
{
  "module": "",
  "knowledge_point": "",
  "type": "",
  "difficulty": "easy/medium/hard",
  "quality_score": 0.0
}

文本：
……

模型返回：

{
  "module": "函数与导数",
  "knowledge_point": "导数判断函数单调性",
  "type": "method",
  "difficulty": "medium",
  "quality_score": 0.88
}


三、用什么方法向量化？

常见有两类。

1. 使用云端 embedding API

比如 OpenAI、通义、智谱、火山、百度等 embedding 模型。

伪代码：

text = "导数判断函数单调性的方法是……"

embedding = embedding_model.embed(text)

优点：

效果好，不需要本地显卡

缺点：

需要 API Key，有调用成本三、用什么方法向量化？

常见有两类。

1. 使用云端 embedding API

比如 OpenAI、通义、智谱、火山、百度等 embedding 模型。

伪代码：

text = "导数判断函数单调性的方法是……"

embedding = embedding_model.embed(text)

优点：

效果好，不需要本地显卡

缺点：

需要 API Key，有调用成本